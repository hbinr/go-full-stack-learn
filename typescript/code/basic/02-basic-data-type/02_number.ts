/**
 * 数字类型学习，内容要点：
 * 1. 定义语法 let one: number = 1
 * 2. 关键字为 number，在Go中有很多种int|uint|int8|uint8|int32|uint32|int64|uint64，还有浮点型:float32|float64等
 * 3. 和 JavaScript 一样，TypeScript 里的所有数字都是浮点数， 这些浮点数的类型是 number。 
 * 4. 除了支持十进制和十六进制字面量，TypeScript 还支持 ECMAScript 2015中引入的二进制和八进制字面量
 * 
 */
  
let decLiteral: number = 20 // 10进制
let hexLiteral: number = 0x14 // 16进制
let octalLiteral: number = 0o24 // 8进制 
let binaryLiteral: number = 0b10100 // 2进制

