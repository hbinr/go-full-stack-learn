/**
 * 布尔类型学习，内容要点：
 * 1. 定义语法  let ok: boolean = true
 * 2. 关键字为 boolean，在Go中为bool
 */

 let ok: boolean = true

 if (ok){
     console.log("Hello  ", ok);
 }
 
 
 if (!ok){
     console.log("Hello ", ok);
 }else{
     console.log("Sorry  ",!ok);
 }